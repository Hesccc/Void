# 测试配置 - 全局扫描模式
check_interval: 60
enable_auto_remove: False
notification_type: "webhook"
checkfile_size: 0
excluded_paths: []

# 全局扫描配置
global_scan:
  enabled: True
  scan_paths:
    - "T:\\"    # 测试 Windows 路径

email:
  smtp_host: "smtp.example.com"
  smtp_port: 465
  username: "send@example.com"
  password: "xxxxxxx"
  to: "to@example.com"

webhook:
  url: "https://moviepilot.hesc.info/api/v1/plugin/MsgNotify/send_json?apikey=L4JxAu7j35EsD9FConECNw"

# 测试：配置一个下载器
services:
  - name: "upload-qbittorrent"
    type: "qbittorrent"
    host: "10.10.0.254"
    port: 8080
    username: "admin"
    password: "Hesc1007."
    path_mapping:
      - "T:\\": "/download"
